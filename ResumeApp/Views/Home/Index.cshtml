
@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="bg-gradient-to-b from-blue-50 to-white min-h-screen">
    <!-- Hero Section -->
    <div class="relative overflow-hidden">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
            <div class="text-center">
                <h1 class="text-4xl tracking-tight font-extrabold text-gray-900 sm:text-5xl md:text-6xl">
                    <span class="block">Yapay Zeka ile</span>
                    <span class="block text-blue-600">CV-İş Eşleştirme</span>
                </h1>
                <p class="mt-3 max-w-md mx-auto text-base text-gray-500 sm:text-lg md:mt-5 md:text-xl md:max-w-3xl">
                    Yapay zeka teknolojimiz ile CV'nizi analiz ediyor, en uygun iş fırsatlarını sizin için buluyoruz.
                </p>
                <div class="mt-5 max-w-md mx-auto sm:flex sm:justify-center md:mt-8">
                    <div class="rounded-md shadow">
                        <a href="@Url.Action("Upload", "Resume")" class="w-full flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 md:py-4 md:text-lg md:px-10">
                            CV Yükle
                        </a>
                    </div>
                    <div class="mt-3 rounded-md shadow sm:mt-0 sm:ml-3">
                        <a href="#nasil-calisir" class="w-full flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-md text-blue-600 bg-white hover:bg-gray-50 md:py-4 md:text-lg md:px-10">
                            Nasıl Çalışır?
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @{
        var applications = ViewBag.UserApplications as List<object>;
    }

    @if (applications != null && applications.Any())
    {
        <!-- Başvuru Durumları Section -->
        <div class="py-12 bg-gradient-to-r from-green-50 to-blue-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="text-center mb-12">
                    <h2 class="text-3xl font-extrabold text-gray-900">
                        🎯 Başvuru Durumlarınız
                    </h2>
                    <p class="mt-4 text-lg text-gray-600">
                        Son başvurularınızın durumunu takip edin
                    </p>
                </div>

                <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
                    @foreach (dynamic application in applications)
                    {
                        var statusClass = application.Status == 0 ? "bg-yellow-100 border-yellow-200 text-yellow-800" :
                                         application.Status == 1 ? "bg-blue-100 border-blue-200 text-blue-800" :
                                         application.Status == 2 ? "bg-green-100 border-green-200 text-green-800" :
                                         "bg-red-100 border-red-200 text-red-800";
                        
                        var statusIcon = application.Status == 0 ? "⏳" :
                                        application.Status == 1 ? "👀" :
                                        application.Status == 2 ? "🎉" :
                                        "❌";
                        
                        var statusBgClass = application.Status == 0 ? "bg-yellow-500" :
                                           application.Status == 1 ? "bg-blue-500" :
                                           application.Status == 2 ? "bg-green-500" :
                                           "bg-red-500";
                        
                        <div class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow duration-300">
                            <div class="@statusBgClass h-2"></div>
                            <div class="p-6">
                                <div class="flex items-start justify-between">
                                    <div class="flex-1">
                                        <h3 class="text-lg font-semibold text-gray-900 mb-2">
                                            @application.JobTitle
                                        </h3>
                                        <p class="text-gray-600 mb-3">
                                            <svg class="inline w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-4m-5 0H9m0 0H5m0 0h2M7 7h10M7 11h10M7 15h10"></path>
                                            </svg>
                                            @application.CompanyName
                                        </p>
                                        <p class="text-sm text-gray-500 mb-4">
                                            <svg class="inline w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3a2 2 0 012-2h4a2 2 0 012 2v4m-6 0V7a2 2 0 012-2h4a2 2 0 012 2v0M8 7v13a2 2 0 002 2h4a2 2 0 002-2V7"></path>
                                            </svg>
                                            @((DateTime)application.ApplicationDate).ToString("dd.MM.yyyy")
                                        </p>
                                    </div>
                                    <div class="text-2xl ml-4">
                                        @statusIcon
                                    </div>
                                </div>
                                
                                <div class="flex items-center justify-between">
                                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium @statusClass">
                                        @application.StatusText
                                    </span>
                                    
                                    @if (application.Status == 2)
                                    {
                                        <div class="text-sm text-green-600 font-medium">
                                            🎊 Tebrikler!
                                        </div>
                                    }
                                    else if (application.Status == 3)
                                    {
                                        <div class="text-sm text-gray-500">
                                            💪 Vazgeçmeyin!
                                        </div>
                                    }
                                </div>
                            </div>
                        </div>
                    }
                </div>

                <!-- Tüm Başvuruları Görme Linki -->
                <div class="mt-8 text-center">
                    <a href="@Url.Action("MyApplications")" class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-blue-600 bg-white hover:bg-blue-50 shadow-sm">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                        </svg>
                        Tüm Başvurularımı Görüntüle
                    </a>
                </div>
            </div>
        </div>
    }

    <!-- Features Section -->   
    <div class="py-12 bg-white" id="nasil-calisir">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center">
                <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
                    Nasıl Çalışır?
                </h2>
                <p class="mt-4 text-lg text-gray-500">
                    Üç basit adımda dream job'unuzu bulun
                </p>
            </div>

            <div class="mt-16">
                <div class="grid grid-cols-1 gap-8 md:grid-cols-3">
                    <!-- Step 1 -->
                    <div class="relative">
                        <div class="absolute flex items-center justify-center h-12 w-12 rounded-md bg-blue-500 text-white">
                            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/>
                            </svg>
                        </div>
                        <div class="ml-16">
                            <h3 class="text-lg font-medium text-gray-900">CV Yükleyin</h3>
                            <p class="mt-2 text-base text-gray-500">
                                PDF formatındaki CV'nizi sistemimize yükleyin. Yapay zeka teknolojimiz içeriği analiz etsin.
                            </p>
                        </div>
                    </div>

                    <!-- Step 2 -->
                    <div class="relative">
                        <div class="absolute flex items-center justify-center h-12 w-12 rounded-md bg-blue-500 text-white">
                            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01"/>
                            </svg>
                        </div>
                        <div class="ml-16">
                            <h3 class="text-lg font-medium text-gray-900">AI Analizi</h3>
                            <p class="mt-2 text-base text-gray-500">
                                Yapay zeka sistemi CV'nizdeki yetenekleri, deneyimleri ve tercihleri analiz eder.
                            </p>
                        </div>
                    </div>

                    <!-- Step 3 -->
                    <div class="relative">
                        <div class="absolute flex items-center justify-center h-12 w-12 rounded-md bg-blue-500 text-white">
                            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                            </svg>
                        </div>
                        <div class="ml-16">
                            <h3 class="text-lg font-medium text-gray-900">Eşleştirme</h3>
                            <p class="mt-2 text-base text-gray-500">
                                Size en uygun iş ilanlarını bulur ve eşleştirme oranlarıyla birlikte sunar.
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Stats Section -->
    <div class="bg-blue-50 py-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid grid-cols-1 gap-5 sm:grid-cols-4 mt-5">
                <div class="bg-white overflow-hidden shadow rounded-lg">
                    <div class="px-4 py-5 sm:p-6 text-center">
                        <dt class="text-sm font-medium text-gray-500 truncate">
                            Toplam CV
                        </dt>
                        <dd class="mt-1 text-3xl font-semibold text-blue-600">
                            1,000+
                        </dd>
                    </div>
                </div>
                <div class="bg-white overflow-hidden shadow rounded-lg">
                    <div class="px-4 py-5 sm:p-6 text-center">
                        <dt class="text-sm font-medium text-gray-500 truncate">
                            İş İlanı
                        </dt>
                        <dd class="mt-1 text-3xl font-semibold text-blue-600">
                            500+
                        </dd>
                    </div>
                </div>
                <div class="bg-white overflow-hidden shadow rounded-lg">
                    <div class="px-4 py-5 sm:p-6 text-center">
                        <dt class="text-sm font-medium text-gray-500 truncate">
                            Başarılı Eşleşme
                        </dt>
                        <dd class="mt-1 text-3xl font-semibold text-blue-600">
                            750+
                        </dd>
                    </div>
                </div>
                <div class="bg-white overflow-hidden shadow rounded-lg">
                    <div class="px-4 py-5 sm:p-6 text-center">
                        <dt class="text-sm font-medium text-gray-500 truncate">
                            Mutlu Kullanıcı
                        </dt>
                        <dd class="mt-1 text-3xl font-semibold text-blue-600">
                            2,000+
                        </dd>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- CTA Section -->
    <div class="bg-white">
        <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:py-16 lg:px-8">
            <div class="bg-blue-600 rounded-lg shadow-xl overflow-hidden lg:grid lg:grid-cols-2 lg:gap-4">
                <div class="pt-10 pb-12 px-6 sm:pt-16 sm:px-16 lg:py-16 lg:pr-0 xl:py-20 xl:px-20">
                    <div class="lg:self-center">
                        <h2 class="text-3xl font-extrabold text-white sm:text-4xl">
                            <span class="block">Hazır mısınız?</span>
                            <span class="block">Hemen CV'nizi yükleyin.</span>
                        </h2>
                        <p class="mt-4 text-lg leading-6 text-blue-200">
                            Yapay zeka destekli sistemimiz ile hayalinizdeki işi bulmanıza yardımcı olalım.
                        </p>
                        <a href="@Url.Action("Upload", "Resume")" class="mt-8 bg-white border border-transparent rounded-md shadow px-6 py-3 inline-flex items-center text-base font-medium text-blue-600 hover:bg-blue-50">
                            Hemen Başla
                        </a>
                    </div>
                </div>
                <div class="relative -mt-6 aspect-w-5 aspect-h-3 md:aspect-w-2 md:aspect-h-1">
                    <div class="transform translate-x-6 translate-y-6 rounded-md object-cover object-left-top sm:translate-x-16 lg:translate-y-20">
                        <div class="animate-pulse flex space-x-4">
                            <div class="flex-1 space-y-4 py-1">
                                <div class="h-4 bg-blue-400 rounded w-3/4"></div>
                                <div class="space-y-2">
                                    <div class="h-4 bg-blue-400 rounded"></div>
                                    <div class="h-4 bg-blue-400 rounded w-5/6"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Chatbot Button -->
<div class="fixed bottom-4 right-4 z-50">
    <button onclick="toggleChat()" class="bg-blue-600 text-white rounded-full p-4 shadow-lg hover:bg-blue-700 transition-colors">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z" />
        </svg>
    </button>
</div>

<!-- Chatbot Modal -->
<div id="chatModal" class="fixed bottom-20 right-4 w-96 bg-white rounded-lg shadow-xl hidden z-50">
    <div class="p-4 border-b border-gray-200 bg-blue-600 rounded-t-lg">
        <div class="flex justify-between items-center">
            <h3 class="text-lg font-semibold text-white">CV Asistanı</h3>
            <button onclick="toggleChat()" class="text-white hover:text-gray-200">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
    </div>
    <div id="chatMessages" class="h-96 overflow-y-auto p-4 space-y-4">
        <!-- Initial message -->
        <div class="flex items-start">
            <div class="flex-shrink-0">
                <div class="w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center">
                    <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                </div>
            </div>
            <div class="ml-3 bg-gray-100 rounded-lg py-2 px-4 max-w-[80%]">
                <p class="text-sm text-gray-900">Merhaba! Ben CV Asistanınız. Size nasıl yardımcı olabilirim?</p>
            </div>
        </div>
    </div>
    <div class="p-4 border-t border-gray-200">
        <div class="flex space-x-2">
            <input type="text" id="messageInput" 
                   class="flex-1 px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" 
                   placeholder="Mesajınızı yazın...">
            <button id="sendButton" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />
                </svg>
            </button>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function toggleChat() {
            const modal = document.getElementById('chatModal');
            modal.classList.toggle('hidden');
        }

        function appendMessage(message, isBot) {
            const chatMessages = document.getElementById('chatMessages');
            const messageDiv = document.createElement('div');
            messageDiv.className = 'flex items-start';

            const iconDiv = document.createElement('div');
            iconDiv.className = 'flex-shrink-0';
            iconDiv.innerHTML = `
                <div class="w-8 h-8 rounded-full ${isBot ? 'bg-blue-500' : 'bg-gray-500'} flex items-center justify-center">
                    <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                              d="${isBot ? 'M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z' : 'M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z'}" />
                    </svg>
                </div>
            `;

            const messageContent = document.createElement('div');
            messageContent.className = `ml-3 ${isBot ? 'bg-gray-100' : 'bg-blue-50'} rounded-lg py-2 px-4 max-w-[80%]`;
            messageContent.innerHTML = `<p class="text-sm text-gray-900">${message}</p>`;

            messageDiv.appendChild(isBot ? iconDiv : messageContent);
            messageDiv.appendChild(isBot ? messageContent : iconDiv);
            
            chatMessages.appendChild(messageDiv);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }

        async function sendMessage() {
            const messageInput = document.getElementById('messageInput');
            const message = messageInput.value.trim();
            
            if (!message) {
                return;
            }

            try {
                // Input'u temizle
                messageInput.value = '';

                const token = document.querySelector('input[name="__RequestVerificationToken"]').value;

                const response = await fetch('/Home/SendMessage', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'X-Requested-With': 'XMLHttpRequest',
                        'RequestVerificationToken': token
                    },
                    body: JSON.stringify({
                        content: message,
                        __RequestVerificationToken: token
                    })
                });

                const data = await response.json();
                
                if (data.success) {
                    // Veritabanından tüm mesajları yeniden yükle
                    setTimeout(() => {
                        loadChatMessages();
                    }, 200); // Kısa bir delay ile veritabanına kayıt tamamlanana kadar bekle
                } else {
                    appendMessage('Üzgünüm, bir hata oluştu: ' + (data.error || 'Bilinmeyen hata'), true);
                }
            } catch (error) {
                console.error('Error:', error);
                appendMessage('Üzgünüm, bir hata oluştu. Lütfen tekrar deneyin.', true);
            }
        }

        // Global fonksiyon - diğer sayfalardan çağrılabilir
        function updateChatMessages() {
            // Sohbet mesajlarını yenile
            loadChatMessages();
        }

        async function loadChatMessages() {
            try {
                const response = await fetch('/Home/GetChatMessages');
                if (response.ok) {
                    const data = await response.json();
                    const chatMessages = document.getElementById('chatMessages');
                    
                    // Mevcut mesajları temizle (ilk mesaj hariç)
                    const firstMessage = chatMessages.firstElementChild;
                    chatMessages.innerHTML = '';
                    chatMessages.appendChild(firstMessage);
                    
                    // Yeni mesajları ekle
                    if (data.messages && data.messages.length > 0) {
                        data.messages.forEach(message => {
                            appendMessage(message.content, message.isFromBot);
                        });
                    }
                }
            } catch (error) {
                console.error('Mesajlar yüklenirken hata:', error);
            }
        }

        // Event Listeners
        document.addEventListener('DOMContentLoaded', function() {
            // Mevcut mesajları yükle
            loadChatMessages();
            
            // URL parametresini kontrol et
            const urlParams = new URLSearchParams(window.location.search);
            if (urlParams.get('openChat') === 'true') {
                // Sohbeti otomatik aç
                setTimeout(() => {
                    toggleChat();
                }, 500);
            }
            
            const messageInput = document.getElementById('messageInput');
            const sendButton = document.getElementById('sendButton');

            if (messageInput) {
                messageInput.addEventListener('keypress', function(event) {
                    if (event.key === 'Enter') {
                        event.preventDefault();
                        sendMessage();
                    }
                });
            }

            if (sendButton) {
                sendButton.addEventListener('click', function(event) {
                    event.preventDefault();
                    sendMessage();
                });
            }
        });
    </script>
}

@* CSRF Token *@
@Html.AntiForgeryToken()
